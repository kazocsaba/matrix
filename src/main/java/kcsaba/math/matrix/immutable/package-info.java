/**
 * Immutable matrix implementation. Create instances using {@link kcsaba.math.matrix.immutable.ImmutableMatrixFactory}.
 * Subclasses of {@link kcsaba.math.matrix.immutable.ImmutableMatrix} are guaranteed to always contain
 * the same elements as when they were created.
 */
package kcsaba.math.matrix.immutable;
